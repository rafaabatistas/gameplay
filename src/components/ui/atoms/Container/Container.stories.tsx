import { storiesOf } from '@storybook/react-native';
import React from 'react';
import { Container } from './Container';

storiesOf('Container', module).add('Basic', () => <Container>Screen</Container>);
