import { storiesOf } from '@storybook/react-native';
import React from 'react';
import { LoadingSpinner } from './LoadingSpinner';

storiesOf('LoadingSpinner', module).add('Basic', () => <LoadingSpinner />);
