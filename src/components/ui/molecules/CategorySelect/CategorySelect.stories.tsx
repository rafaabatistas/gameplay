import { storiesOf } from '@storybook/react-native';
import React from 'react';
import { CategorySelect } from './CategorySelect';

storiesOf('CategorySelect', module).add('Basic', () => <CategorySelect />);
